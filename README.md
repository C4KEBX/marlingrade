# Marlin Grade

Marlin Grade is a demo build of a farm-scoring product: the `marlin_engine` Python package ports the FarmSignal scoring logic and builds a self-contained demo bundle from the local DuckDB data inventory. For the full scope, architecture, task breakdown, and build steps, see [`docs/DEMO_BUILD_SPEC.md`](docs/DEMO_BUILD_SPEC.md).
